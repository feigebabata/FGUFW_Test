# Unity面试知识

语法知识,性能优化

***

## 目录
  - [C#基础](#c基础)
    - [面向对象](#面向对象)
    - [内存堆栈](#内存堆栈)
    - [Struct和Class](#struct和class)
    - [对象传递](#对象传递inrefout)
    - [编码习惯](#编码习惯)
  - [Unity基础](#unity基础)
    - [MonoBehaviour生命周期](#monobehaviour生命周期)
    - [DOTS](#dots)
    - [AssetBundle](#assetbundle)
    - [ComputeShader](#computeshader)
    - [Editor开发](#editor开发)
  - [性能优化](#性能优化)
    - [装箱拆箱](#装箱拆箱)
    - [对象缓存](#对象缓存)
    - [UGUI](#ugui)
    - [Physics](#physics)
    - [渲染合批](#渲染合批)
    - [场景](#场景)
    - [其他](#其他)
  - [问答](#问答)
    - [自我介绍](#自我介绍)
    - [你遇到的困难](#你遇到的困难)
    - [你的优缺点](#你的优缺点)


***

## C#基础

  ### 面向对象
   - 封装:控制给外部可用哪些接口
   - 继承:复用与扩展已有代码模块
   - 多态:同一个操作，作用于不同的对象，会产生不同的结果

  ### 内存堆栈
   - 堆是按需申请、手动分配的，需要用户手动回收。一般速度比栈慢；存储地址通常是链式的，内存较大不会溢出
     堆内存使用完后未主动释放就会一直占用程序内存 造成内存泄漏(尤其是非通过runtime申请的内存)

   - 栈是程序运行时自动拥有的一小块内存，用于局部变量的存放或者函数调用栈的保存
     遵守后进先出模式,顺序存储,一旦内存申请过多(如递归层太深)会导致栈溢出(Stack Overflow),被系统杀死
     注意：对象的引用指针是分配在栈上的

  ### Struct和Class
   - Struct变量直接存储数据,默认传递时是对数据的拷贝
     可以通过ref,out关键字传递变量的地址
     不能被继承但能实现接口 通过接口引用值类型时会装箱
   - Class变量存储数据的地址,默认传递也只需要传递地址
     可以通过in关键字限制对数据的修改 常用类型中string,interface,数组都是引用类型

  ### 对象传递in,ref,out
   - in: 只读
   - ref: 传递前必须已赋值
   - out: 传递后必须被修改

  ### 编码习惯

   - 为减少代码的量 方便维护会对常用功能进行封装
   - 各个语言库其实就是官方封装好的功能集

   1. 严谨的API的输入数据都是通过参数传进来的 结果是返回出去的 不在函数中获取和修改域外参数
   2. 函数对输入的参数需要异常检测 担心影响性能可以通过宏控制(可以使用Assert)
   3. 对现有类型封装时使用[Extensions][Extensions]扩展函数 来使你的代码更加优雅
   4. 使用`partial`分割类的不同功能 (状态/动画/编辑器) 避免代码文件行数过多

## Unity基础

  ### MonoBehaviour生命周期
   - 每个对象Awake>OnEnable执行完后才执行Start
   - Awake/Start/Update/FixedUpdate都是无序执行
   - OnDestroy深度优先 最外层无序 子节点从上往下

  ### DOTS
   - 面向数据开发技术栈,目前限制只能在值类型和自定义的数据容器使用
   - JobSystem: 多线程利用方案 旨在一帧内把同类问题并行处理掉
   - BurstCompiler: 编译优化方案,利用LLVM,SIMD等技术提升效率
   - ECS: 组件按Archertype(SOA)存储 提高缓存命中率

  ### AssetBundle
   - Resources是从一个缺省打包进程序包的AssetBundle加载资源的
   - AssetBundle的异步加载的卡顿是因为加载内容相关的Shader解析,Texture/Mesh上传到GPU,Component的序列化等都是需要在主线程完成的
   - 资源合理的划分到不同的AB包 减少更新时的下载内容
  
  ### ComputeShader
   - 将计算内容提交给GPU处理,提高处理效率和减轻CPU负载,但不是所有设备都支持
   - 数据传递通过Texture/ComputeBuffer
   - 常用于计算和后期处理
  
  ### Editor开发
   - 使用UIToolkit可以方便搭建独立编辑器页面,编辑器内扩展使用Editor
   - 继承SettingsProvider 在`ProjectSettings`新开一个设置界面,并将配置内容存在ProjectSettings文件夹内
   - ScriptableObject可以转换成Editor对象绘制在编辑器上 从而直接修改
   - 自定义编辑器可实时修改字段 使用SerializedObject将对象绑定到Editor组件并指定`bindingPath=字段名`

***

## 性能优化

  ### 装箱拆箱
   - 装修: 值类型转换成引用类型
   - 拆箱: 引用类型转换成值类型
   - 常发生在object/interface与结构体互相转换,因为会导致内存的频繁操作需要尽量避免

  ### 对象缓存
   - 对需要频繁使用对象进行缓存,避免频繁的销毁和创建(如对象池)
   - 字符串拼接使用StringBuilder 不要直接拼接字符串
   - GetComponent后存起来 尽量少使用
   - 加载的Texture/Mesh等资源都是独一份不管加载几次,但修改开启**Read/Wirte**的资源会对资源拷贝

  ### UGUI
   - 每个Canvas都相当于一个独立Mesh渲染,Canvas内的任何移动/层级变换/字段修改等都会导致重绘
   - 动静分离 将需要频繁更新的UI对象放到一个Canvas下
   - Canvas层级不要嵌套太深
   - Canvas的RenderMode设置**Screen Space - Overlay**就不需要摄像机了
   - 禁用所有不需要交互UI组件的**Raycast Target**
   - 全屏UI时可以关闭相机并调低帧率(减少移动端的能耗)
   - Mask换Rect Mask 2D,Mask会影响合批

  ### Physics
   - 严格控制`Layer Collision Matrix`碰撞矩阵 去除不必要的碰撞关系
   - 尽量简化碰撞体形状 基础碰撞体优于**Mesh Collider**
   - 只在FixedUpdate修改物理组件
   - 移动带刚体对象时使用刚体组件的移动函数
   - 可以修改`Fixed Timestep`降低FixedUpdate的执行频率来减少消耗(注意别导致**碰撞检测穿透**)

  ### 渲染合批
   - **Draw Call**是CPU调用图形绘制接口的一次绘制操作,对每个对象都调用一次明显太耗费性能了所以就需要批处理`DC Batching`
   - 动态合批: 对使用用一个材质的mesh会自动合批(但有顶点数限制 超过了也会拆开提交),非等比缩放会导致合批失败
     可以使用**MaterialPropertyBlock**来修改材质字段而不打断合批
   - UI需要手动打图集

  ### 场景
   - LOD: 多层次细节 不同距离使用不同面数的模型 减少渲染面数
   - Mipmap: 不同距离使用不同像素的贴图
   - 大量GameObject.SetActive的消耗不低 不想渲染可以将对象移动到相机外或者修改Layer为不渲染的
   - 对象池: 对于子弹,粒子等重复性高的对象进行复用
   - 控制光源数量,减少动态阴影,开启遮挡剔除


  ### 其他
   - `VSync`垂直同步会影响更新节奏 看需求觉得是否关闭

***

## 问答

  ### 自我介绍
   - 以下是一些面试时自我介绍的要点：
   1. 开场问候：礼貌地打招呼。
   2. 个人基本信息：简要介绍姓名、年龄、毕业院校等。
   3. 相关经验和技能：重点阐述与岗位相关的经历和能力。
   4. 职业目标：表达对该岗位的兴趣和职业规划。
   5. 突出优势：提及自己的亮点和独特之处。
   6. 结束语：表达感谢并展示积极态度。
   - 你可以根据实际情况进行调整和补充，尽量做到简洁明了、自信大方。以下是一个简单的自我介绍示例供你参考：
   - “您好，我是[姓名]，毕业于[院校]。我具备[相关技能]，曾在[公司/项目]中担任[职位]，积累了[相关经验]。我对[应聘岗位]非常感兴趣，希望能在贵公司发挥自己的才能，实现个人价值。感谢您给我这个机会进行自我介绍。”

  ### 你遇到的困难
   - STAR面试技巧
   - S：场景，事件的背景
   - T：任务，当时主要的任务
   - A：行动，采取了什么行动
   - R：结果，结果如何，反思与收获
   * ~~在使用Aseprite像素动画时 默认导入的动画原点在中间 而且不方便查看修改后的效果 每次动画修改源文件都会重置 我通过查看Aseprite编辑器发现有鼠标的坐标信息 然后让动画把鼠标放在动画的原点把坐标记录在配置表中 我修改了Aseprite插件中的导入代码 根据配置表中的坐标修改动画原点的坐标 这样只需要动画在制作动画时配置下坐标就行~~
  
  ### 你的优缺点
   - 在谈论缺点时，策略是选择那些不会直接影响工作表现，或者你正在积极改进的方面，并且最好能转化为积极的表述。
   * ~~喜欢尝试新技术 提高了功能效率或编码可维护性,但忍不住顺手把以前的模块一起修改~~
***

[返回顶部](#unity面试知识)
